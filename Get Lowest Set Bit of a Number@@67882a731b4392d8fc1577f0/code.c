#include <stdio.h>

int main() {
    int num,count;
    
    
    scanf("%d", &num);
    
    while(num!=0)
    {
        a=a>>1;
        if(a&1==1)
        {
            count+=1
        }
    }
    
    printf("%d",count);
    
    return 0;
}
